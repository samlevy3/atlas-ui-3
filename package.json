{
  "name": "atlas-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Atlas UI - Cloud Foundry build wrapper",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
